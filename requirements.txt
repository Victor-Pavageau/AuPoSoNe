# Core dependencies
requests>=2.32.5
python-dotenv>=1.1.1
selenium>=4.35.0

# Note: FFmpeg is required but installed separately as a system dependency
# Install FFmpeg for your OS:
# - Windows: winget install --id=Gyan.FFmpeg --version 7.1.1
